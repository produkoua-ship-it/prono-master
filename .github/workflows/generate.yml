name: Generate Daily Combines

on:
  schedule:
    - cron: "0 3 * * *"  # Une seule fois par jour à 03h00 UTC
  workflow_dispatch: # Déclenchement manuel uniquement (pas de push)

jobs:
  generate:
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY }}
      ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm ci

      - name: Install WebSocket dependency
        run: npm install ws

      - name: Run verification script
        run: node scripts/verificateur_resultats.js

      - name: Run generate script
        run: node scripts/generateur_combines.js
